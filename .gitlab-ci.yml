# All available Hugo versions are listed here: https://gitlab.com/pages/hugo/container_registry
image: registry.gitlab.com/pages/hugo:latest

# variables:
#   GIT_SUBMODULE_STRATEGY: recursive

# test:
#   script:
#   - hugo
#   except:
#   - master

pages:
  script:
  - hugo --source exampleSite --baseURL https://cyannyan.com/ --themesDir ../.. --destination public
  - mv exampleSite/public public
  artifacts:
    paths:
    - public
  only:
  - master
